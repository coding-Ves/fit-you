import RegistrationHealthForm from '../../components/Authentication/Registration/RegistrationForms/RegisterHealthForm';

const RegisterHealth = () => {
    return <RegistrationHealthForm />;
};

export default RegisterHealth;
