// import PropTypes from 'prop-types';

const GoalsHistoryTable = ({ goals }) => {
    return <div>GoalsHistoryTable</div>;
};

GoalsHistoryTable.propTypes = {
    // goals: PropTypes.array.isRequired,
};

export default GoalsHistoryTable;
